# LegacyTimelineBlock

Bloc timeline réutilisable pour sections claires/sombres.

## Props
- `title: string` (requis)
- `items: Array` (requis, inclut `title`, `time`, `description`, `tags[]`, `icon`, `color`)
- `dark?: boolean` (défaut `false`)

## Events
- Aucun.

## Slots
- Aucun.
