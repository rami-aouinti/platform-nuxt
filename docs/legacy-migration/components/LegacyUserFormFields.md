# LegacyUserFormFields

Groupe de champs utilisateur réutilisable (ex: onboarding/profil).

## Props
- `value: Record<string, any>` (requis)
- `fields: Array` (requis, `model`, `label`, optionnel `type`, `sm`)

## Events
- `input-field({ key, value })` sur saisie d'un champ.

## Slots
- Aucun.
