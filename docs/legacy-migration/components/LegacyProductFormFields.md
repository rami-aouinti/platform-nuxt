# LegacyProductFormFields

Groupe de champs produit réutilisable (ex: création/édition produit).

## Props
- `value: Record<string, any>` (requis)
- `fields: Array` (requis, `model`, `label`, optionnel `type`, `cols`, `md`)

## Events
- `input-field({ key, value })` sur saisie d'un champ.

## Slots
- Aucun.
